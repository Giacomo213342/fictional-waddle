#!/usr/bin/make -f
# See debhelper(7) (uncomment to enable)
# output every command that modifies files on the build system.
#DH_VERBOSE = 1

%:
	dh $@

build:
	ln -s /opt/polycule/polycule polycule
	flutter gen-l10n
	flutter build linux
